Server setup (quick):
1. npm install
2. Provide FCM service account JSON via env var FCM_SERVICE_ACCOUNT_JSON or file.
3. Provide APNs key (.p8) contents via env var APN_KEY_P8 and APN_KEY_ID, APN_TEAM_ID, APN_TOPIC.
4. Start: node index.js
